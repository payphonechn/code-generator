package ${package}.${moduleName}.mapper;

import ${package}.${moduleName}.entity.${className}Entity;
import org.apache.ibatis.annotations.Mapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Mapper{


    ${className}Entity queryByPrimaryKey(${pk.attrType} ${pk.attrname});

    //根据参数查询列表
    List<${className}Entity> queryList(${className}Entity entity);

    void save(${className}Entity entity);

    void update(${className}Entity entity);

    void delete(${pk.attrType} ${pk.attrname});
	
}
